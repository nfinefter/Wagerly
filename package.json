{
  "name": "wagerly",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "npm run dev -w @wagerly/web",
    "build": "turbo build",
    "build:web": "npm run build -w @wagerly/web",
    "vercel-build": "npm run build -w @wagerly/web",
    "lint": "turbo lint",
    "db:push": "echo 'Run supabase/migrations/20250601000000_init.sql in Supabase SQL Editor'"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "@types/react": "19.1.6",
    "@types/react-dom": "19.1.5"
  }
}
